const input = prompt("Nhập một ký tự: ");

if ( input.indexOf(1||2||3||4||5||6||7||8||9||0)==-1) {
    document.write(`Ký tự ${input} là chữ cái`);
} else {
    document.write("Không phải chữ cái");
}
